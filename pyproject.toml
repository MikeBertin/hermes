[project]
name = "hermes"
version = "0.1.0"
description = "Bitcoin from first principles — a from-scratch implementation, visualised."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
